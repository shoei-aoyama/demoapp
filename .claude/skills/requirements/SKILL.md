---
name: requirements
description: 人間の要望を対話で整理し、承認後に要件ドキュメントを1件作成する。
argument-hint: "[要望の概要]"
disable-model-invocation: true
allowed-tools:
  - Read
  - Glob
  - Grep
  - Bash
  - Write
disallowed-tools:
  - Edit
  - NotebookEdit
  - Skill
---

# requirements

## 目的

プロジェクト全体の大元となる要件を、最初の1回だけ整理し、承認後にドキュメントを1件作成する。

個別の新機能・改善・保守の要件定義には使用しない。それらは`/plan-docs`を起点とする。

Issue起票、技術設計、コード調査、実装は行わない。

## 手順

1. 関連する既存ドキュメントを確認する。
2. 不足事項を一度に2〜3問ずつ質問する。
3. 回答を要件案として提示する。
4. GOサイン後、要件ドキュメントを作成する。
5. 作成結果と次の工程を報告する。

質問は次の項目から必要なものだけを選ぶ。

- 目的・背景
- 利用者
- 必要な挙動
- 受け入れ条件
- 対象外
- 制約

既存情報を再質問せず、不明点を推測で補わない。曖昧な回答には短い例や選択肢を提示する。

## 要件案

```markdown
# 要件名

## 目的・背景

-

## 利用者

-

## 必要な挙動

-

## 受け入れ条件

- [ ]

## 対象外

-

## 制約

-

## 未決事項

- なし
```

受け入れ条件は、完成後に確認できる状態として記載する。実装方法やファイル名は含めない。

各項目は大枠の合意に必要な粒度に留める。エッジケースの網羅や文言レベルの詳細化は`/more-requirements`で行うため、この時点で深追いしない。

## 承認後

GOサインを得た場合のみ、承認された要件案を1ファイル作成する。

- 新機能：`docs/requirements/`
- 保守・修正：`docs/maintenance/`

ファイル名は内容を表す短いkebab-caseにする。同じ内容のファイルがある場合は上書きせず確認する。

## 禁止事項

- 承認前のファイル作成
- Issue起票
- 仕様の推測や機能の先回り
- 技術設計、コード変更、ブランチ操作
- 既存ファイルの変更
- 複数ドキュメントの作成

`Write`は承認後の新規作成だけに使用する。`Bash`は情報確認だけに使用する。

## 完了報告

- 作成したファイル
- 要件の概要
- 未決事項
- 次の工程

次は `/more-requirements <要件ドキュメントのパス>` で要件を深掘りする。

以降、個別の新機能・改善・保守の要件定義は`/plan-docs`から始める。

## 参照

- `.claude/rules/project-principles.md`
- `docs/requirements/`
- `docs/maintenance/`
